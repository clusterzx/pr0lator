import { APIRequester } from "./index";
import * as Types from "../common-types";
export type Cookies = Record<string, unknown>;
export declare class NodeRequester implements APIRequester {
    cookies: Cookies | false;
    private readonly baseUrl;
    private oAuthAccessToken;
    private readonly apiUrl;
    private static readonly headers;
    constructor(cookies: Cookies | false, baseUrl: string);
    setOAuthAccessToken(accessToken: string): void;
    static create(baseUrl?: string, cookies?: Cookies): APIRequester;
    get<T>(path: string, queryString?: Types.KeyValue<any>): Promise<T>;
    post<T>(path: string, body?: Types.KeyValue<any>, ignoreNonce?: boolean): Promise<T>;
    private getMeCookie;
}
